version: '3'

networks:
  app-network:
    driver: bridge

services:
  carbohelm:
    image: carbo-helm
    networks:
      - app-network
    build:
      context: .
      dockerfile: dockerfile
    env_file: .env
    ports:
      - 3000:3000
    